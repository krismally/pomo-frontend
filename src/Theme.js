import { createTheme } from "@mui/material/styles"

const theme = createTheme({
    palette: {
      primary: {
        main: '#5F876F',
      },
      secondary: {
        main: '#79929F',
      },
      background: {
        default: '#516772',
        paper: '#516772',
      },
      text: {
        primary: '#FDFDFD',
      },
    },
  })
  
export default theme;